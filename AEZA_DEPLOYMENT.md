# 🚀 Развертывание Water Reminder Bot на Aeza

Полное пошаговое руководство по развертыванию Telegram-бота на облачном сервисе Aeza.

---

## 📋 Содержание

1. [Предварительная подготовка](#предварительная-подготовка)
2. [Аренда VPS на Aeza](#аренда-vps-на-aeza)
3. [Первоначальная настройка сервера](#первоначальная-настройка-сервера)
4. [Установка и запуск бота](#установка-и-запуск-бота)
5. [Настройка автозапуска](#настройка-автозапуска)
6. [Мониторинг и обслуживание](#мониторинг-и-обслуживание)
7. [Обновление бота](#обновление-бота)
8. [Резервное копирование](#резервное-копирование)
9. [Устранение неполадок](#устранение-неполадок)

---

## 📦 Предварительная подготовка

### Что вам потребуется:

1. ✅ **Telegram Bot Token**
   - Получите у [@BotFather](https://t.me/BotFather)
   - Команда: `/newbot`
   - Сохраните токен в безопасном месте

2. ✅ **Аккаунт на Aeza**
   - Зарегистрируйтесь на [aeza.net](https://aeza.net)
   - Пополните баланс (минимум ~150-200 руб/месяц)

3. ✅ **SSH клиент**
   - Windows: PowerShell или [PuTTY](https://www.putty.org/)
   - Linux/macOS: встроенный терминал

4. ✅ **Файлы бота на локальном компьютере**
   - Убедитесь, что бот работает локально
   - Проверьте наличие всех файлов

### Чеклист файлов проекта:

```
water-reminder-bot/
├── app/                          ✅ Главный модуль
├── .env.example                  ✅ Пример конфигурации
├── .gitignore                    ✅ Исключения для Git
├── requirements.txt              ✅ Зависимости Python
├── water_reminder_bot.py         ✅ Точка входа
├── Dockerfile                    ✅ Docker образ (опционально)
├── docker-compose.yml            ✅ Docker Compose (опционально)
└── README.md                     ✅ Документация
```

---

## 🖥️ Аренда VPS на Aeza

### Шаг 1: Регистрация и пополнение баланса

1. Перейдите на [aeza.net](https://aeza.net)
2. Зарегистрируйтесь или войдите в аккаунт
3. Пополните баланс через удобный способ оплаты

### Шаг 2: Выбор тарифа

**Рекомендуемая конфигурация для бота:**

| Параметр | Минимум | Рекомендуется |
|----------|---------|---------------|
| CPU      | 1 ядро  | 1 ядро        |
| RAM      | 512 MB  | 1 GB          |
| SSD      | 10 GB   | 15 GB         |
| Цена     | ~150₽/мес | ~250₽/мес   |

### Шаг 3: Создание VPS

1. В панели управления нажмите **"Создать VPS"**
2. Выберите параметры:
   - **Локация**: Любая (рекомендуется ближайшая к вам)
   - **ОС**: **Ubuntu 22.04 LTS** (обязательно!)
   - **Тариф**: Согласно таблице выше
   - **Имя**: `water-bot` (или любое другое)

3. Нажмите **"Создать"**

4. **Сохраните данные для подключения:**
   ```
   IP-адрес: XXX.XXX.XXX.XXX
   Логин: root
   Пароль: XXXXXXXXXX
   ```

⚠️ **ВАЖНО:** Сохраните эти данные в надежном месте!

---

## 🔧 Первоначальная настройка сервера

### Шаг 1: Подключение к серверу

#### Windows (PowerShell):

```powershell
ssh root@YOUR_SERVER_IP
```

#### Linux/macOS:

```bash
ssh root@YOUR_SERVER_IP
```

При первом подключении появится запрос:
```
Are you sure you want to continue connecting (yes/no)?
```
Введите `yes` и нажмите Enter.

Затем введите пароль (символы не отображаются при вводе).

### Шаг 2: Обновление системы

После успешного подключения выполните:

```bash
# Обновление списка пакетов
apt update

# Обновление установленных пакетов
apt upgrade -y

# Перезагрузка (если требуется)
# reboot
```

### Шаг 3: Установка необходимого ПО

```bash
# Установка Python 3, pip, venv и Git
apt install python3 python3-pip python3-venv git nano curl -y

# Проверка версий
python3 --version
pip3 --version
git --version
```

Ожидаемый результат:
```
Python 3.10.x или выше
pip 22.x или выше
git 2.x
```

### Шаг 4 (Опционально): Создание отдельного пользователя

Для повышения безопасности создайте отдельного пользователя:

```bash
# Создание пользователя
adduser botuser

# Добавление в группу sudo (опционально)
usermod -aG sudo botuser

# Переключение на нового пользователя
su - botuser
```

**Для простоты в этой инструкции мы продолжим от root.**

---

## 🤖 Установка и запуск бота

### Вариант 1: Через Git (рекомендуется)

#### Шаг 1: Загрузка кода на GitHub

На вашем **локальном компьютере**:

```bash
# Перейдите в папку проекта
cd "C:\My projects\water reminder"

# Инициализация Git (если еще не сделано)
git init

# Добавление всех файлов
git add .

# Первый коммит
git commit -m "Initial commit for deployment"

# Создание репозитория на GitHub
# Перейдите на github.com и создайте ПРИВАТНЫЙ репозиторий

# Связывание с удаленным репозиторием
git remote add origin https://github.com/YOUR_USERNAME/water-reminder-bot.git

# Загрузка на GitHub
git branch -M main
git push -u origin main
```

⚠️ **ВАЖНО:** Убедитесь, что `.env` и `token.txt` в `.gitignore`!

#### Шаг 2: Клонирование на сервер

На **сервере Aeza**:

```bash
# Клонирование репозитория
git clone https://github.com/YOUR_USERNAME/water-reminder-bot.git

# Переход в папку проекта
cd water-reminder-bot

# Проверка содержимого
ls -la
```

### Вариант 2: Через SCP/SFTP (альтернатива)

На **локальном компьютере** (PowerShell):

```powershell
# Упаковка проекта
Compress-Archive -Path "C:\My projects\water reminder\*" -DestinationPath water-bot.zip

# Загрузка на сервер через SCP
scp water-bot.zip root@YOUR_SERVER_IP:/root/
```

На **сервере**:

```bash
# Установка unzip
apt install unzip -y

# Распаковка
unzip water-bot.zip -d water-reminder-bot
cd water-reminder-bot
```

### Шаг 3: Создание виртуального окружения

```bash
# Создание venv
python3 -m venv venv

# Активация
source venv/bin/activate

# После активации вы увидите (venv) в начале строки
```

### Шаг 4: Установка зависимостей

```bash
# Установка всех пакетов из requirements.txt
pip install -r requirements.txt

# Проверка установленных пакетов
pip list
```

Ожидаемые пакеты:
- python-telegram-bot
- APScheduler
- pytz
- python-dotenv
- sqlalchemy

### Шаг 5: Настройка конфигурации

```bash
# Создание .env файла
cp .env.example .env

# Редактирование .env
nano .env
```

В открывшемся редакторе `nano`:

1. Найдите строку `TELEGRAM_BOT_TOKEN=your_bot_token_here`
2. Замените `your_bot_token_here` на ваш реальный токен
3. При необходимости настройте другие параметры

Пример:
```env
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
DEFAULT_TIMEZONE=Europe/Moscow
LOG_LEVEL=INFO
```

**Сохранение в nano:**
- Нажмите `Ctrl+X`
- Нажмите `Y` (Yes)
- Нажмите `Enter`

### Шаг 6: Первый тестовый запуск

```bash
# Запуск бота
python -m app
```

Ожидаемый вывод:
```
--- Запущено с помощью Python версии: 3.10.x ---
2025-XX-XX XX:XX:XX - app.bot - INFO - 🚀 Запуск Water Reminder Bot v2.0
2025-XX-XX XX:XX:XX - app.bot - INFO - ✅ Application настроен и готов к работе
Bot is running...
```

**Протестируйте бота в Telegram:**
1. Откройте вашего бота в Telegram
2. Отправьте команду `/start`
3. Убедитесь, что бот отвечает

**Остановите бота:**
- Нажмите `Ctrl+C`

✅ Если бот работает, переходим к автозапуску!

---

## ⚙️ Настройка автозапуска

Чтобы бот работал 24/7 и автоматически перезапускался при сбоях, настроим `systemd`.

### Шаг 1: Создание systemd service файла

```bash
# Создание файла сервиса
nano /etc/systemd/system/water_bot.service
```

### Шаг 2: Вставка конфигурации

Скопируйте и вставьте следующий текст (замените пути, если нужно):

```ini
[Unit]
Description=Water Reminder Telegram Bot
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/root/water-reminder-bot
Environment="PATH=/root/water-reminder-bot/venv/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin"
ExecStart=/root/water-reminder-bot/venv/bin/python -m app
Restart=always
RestartSec=10
StandardOutput=append:/root/water-reminder-bot/systemd_output.log
StandardError=append:/root/water-reminder-bot/systemd_error.log

[Install]
WantedBy=multi-user.target
```

**Параметры конфигурации:**

- `WorkingDirectory` - путь к проекту
- `ExecStart` - команда запуска бота через venv
- `Restart=always` - автоматический перезапуск при падении
- `RestartSec=10` - пауза 10 секунд перед перезапуском
- `StandardOutput/StandardError` - логи systemd

**Если вы используете другого пользователя (не root):**

Замените:
```ini
User=botuser
WorkingDirectory=/home/botuser/water-reminder-bot
Environment="PATH=/home/botuser/water-reminder-bot/venv/bin:..."
ExecStart=/home/botuser/water-reminder-bot/venv/bin/python -m app
StandardOutput=append:/home/botuser/water-reminder-bot/systemd_output.log
StandardError=append:/home/botuser/water-reminder-bot/systemd_error.log
```

**Сохраните файл:** `Ctrl+X` → `Y` → `Enter`

### Шаг 3: Активация и запуск сервиса

```bash
# Перезагрузка конфигурации systemd
systemctl daemon-reload

# Включение автозапуска при загрузке системы
systemctl enable water_bot

# Запуск сервиса
systemctl start water_bot

# Проверка статуса
systemctl status water_bot
```

**Ожидаемый вывод `status`:**

```
● water_bot.service - Water Reminder Telegram Bot
     Loaded: loaded (/etc/systemd/system/water_bot.service; enabled; vendor preset: enabled)
     Active: active (running) since ...
   Main PID: XXXX (python)
      Tasks: X
     Memory: XX.XM
     CGroup: /system.slice/water_bot.service
             └─XXXX /root/water-reminder-bot/venv/bin/python -m app
```

✅ **`Active: active (running)`** означает, что бот работает!

---

## 📊 Мониторинг и обслуживание

### Основные команды systemd

```bash
# Проверка статуса
systemctl status water_bot

# Остановка
systemctl stop water_bot

# Запуск
systemctl start water_bot

# Перезапуск
systemctl restart water_bot

# Отключение автозапуска
systemctl disable water_bot

# Включение автозапуска
systemctl enable water_bot
```

### Просмотр логов

#### 1. Логи systemd (в реальном времени)

```bash
# Все логи с начала
journalctl -u water_bot

# Последние 100 строк
journalctl -u water_bot -n 100

# В реальном времени (Ctrl+C для выхода)
journalctl -u water_bot -f

# Логи за сегодня
journalctl -u water_bot --since today

# Логи с определенного времени
journalctl -u water_bot --since "2025-10-28 10:00:00"
```

#### 2. Логи бота из файла

```bash
# Последние 50 строк
tail -50 bot_log.txt

# В реальном времени
tail -f bot_log.txt

# Поиск ошибок
grep ERROR bot_log.txt

# Поиск конкретного пользователя
grep "chat_id:123456789" bot_log.txt
```

#### 3. Логи systemd из файлов

```bash
# Стандартный вывод
tail -f systemd_output.log

# Ошибки
tail -f systemd_error.log
```

### Мониторинг ресурсов

```bash
# Использование CPU и памяти ботом
ps aux | grep python

# Общая информация о системе
htop

# Место на диске
df -h

# Размер файлов бота
du -sh /root/water-reminder-bot
du -sh /root/water-reminder-bot/*.db
```

### Проверка работы бота через Telegram

```bash
# Отправьте команду /start в Telegram
# Проверьте логи на наличие активности:
tail -20 bot_log.txt
```

---

## 🔄 Обновление бота

### Через Git (если используете GitHub)

```bash
# Переход в папку проекта
cd /root/water-reminder-bot

# Остановка бота
systemctl stop water_bot

# Получение обновлений с GitHub
git pull origin main

# Активация venv (если нужно обновить зависимости)
source venv/bin/activate

# Обновление зависимостей (если requirements.txt изменился)
pip install -r requirements.txt --upgrade

# Запуск бота
systemctl start water_bot

# Проверка статуса
systemctl status water_bot
```

### Вручную (через SCP/SFTP)

```bash
# 1. Остановка бота
systemctl stop water_bot

# 2. Создание резервной копии
cd /root
tar -czf water-bot-backup-$(date +%Y%m%d).tar.gz water-reminder-bot

# 3. Загрузка новых файлов через SCP (на локальном ПК)
# scp -r "C:\My projects\water reminder\app" root@YOUR_SERVER_IP:/root/water-reminder-bot/

# 4. На сервере: перезапуск
systemctl start water_bot
systemctl status water_bot
```

---

## 💾 Резервное копирование

### Автоматическое резервное копирование

Создайте скрипт для регулярных бэкапов:

```bash
# Создание скрипта
nano /root/backup_bot.sh
```

Вставьте:

```bash
#!/bin/bash
BACKUP_DIR="/root/backups"
BOT_DIR="/root/water-reminder-bot"
DATE=$(date +%Y%m%d_%H%M%S)

# Создание папки для бэкапов
mkdir -p $BACKUP_DIR

# Архивация
tar -czf $BACKUP_DIR/water-bot-$DATE.tar.gz $BOT_DIR

# Удаление старых бэкапов (старше 7 дней)
find $BACKUP_DIR -name "water-bot-*.tar.gz" -mtime +7 -delete

echo "Backup created: water-bot-$DATE.tar.gz"
```

Сохраните и сделайте исполняемым:

```bash
chmod +x /root/backup_bot.sh
```

### Настройка автоматического выполнения (cron)

```bash
# Редактирование cron
crontab -e
```

Добавьте строку (бэкап каждый день в 3:00 ночи):

```
0 3 * * * /root/backup_bot.sh >> /root/backup_bot.log 2>&1
```

Сохраните: `Ctrl+X` → `Y` → `Enter`

### Ручное резервное копирование

```bash
# Создание папки
mkdir -p /root/backups

# Создание архива
cd /root
tar -czf backups/water-bot-manual-$(date +%Y%m%d).tar.gz water-reminder-bot

# Просмотр созданных бэкапов
ls -lh backups/
```

### Скачивание бэкапа на локальный ПК

```powershell
# На локальном ПК (PowerShell)
scp root@YOUR_SERVER_IP:/root/backups/water-bot-*.tar.gz "C:\Backups\"
```

### Восстановление из бэкапа

```bash
# Остановка бота
systemctl stop water_bot

# Удаление текущей версии (осторожно!)
rm -rf /root/water-reminder-bot

# Восстановление из архива
cd /root
tar -xzf backups/water-bot-20251028.tar.gz

# Запуск бота
systemctl start water_bot
systemctl status water_bot
```

---

## 🔍 Устранение неполадок

### Проблема 1: Бот не запускается

**Симптомы:**
```bash
systemctl status water_bot
# Active: failed (Result: exit-code)
```

**Решение:**

```bash
# Проверка логов systemd
journalctl -u water_bot -n 50 --no-pager

# Проверка логов бота
tail -50 /root/water-reminder-bot/bot_log.txt

# Проверка конфигурации
cat /root/water-reminder-bot/.env

# Проверка токена
grep TELEGRAM_BOT_TOKEN /root/water-reminder-bot/.env

# Ручной запуск для диагностики
cd /root/water-reminder-bot
source venv/bin/activate
python -m app
```

**Частые причины:**
- Неверный токен бота
- Отсутствует `.env` файл
- Не установлены зависимости
- Ошибка в пути в `water_bot.service`

---

### Проблема 2: Бот работает, но не отвечает

**Решение:**

```bash
# Проверка статуса
systemctl status water_bot

# Проверка логов на ошибки
grep ERROR /root/water-reminder-bot/bot_log.txt

# Проверка сетевого подключения
ping -c 3 api.telegram.org

# Перезапуск бота
systemctl restart water_bot
```

---

### Проблема 3: Высокая нагрузка на сервер

**Решение:**

```bash
# Проверка использования ресурсов
htop

# Проверка размера логов
ls -lh /root/water-reminder-bot/*.txt
ls -lh /root/water-reminder-bot/*.log

# Очистка больших логов
> /root/water-reminder-bot/bot_log.txt
> /root/water-reminder-bot/systemd_output.log

# Ротация логов (настройка в .env)
nano /root/water-reminder-bot/.env
# LOG_LEVEL=WARNING  # Меньше логов
```

---

### Проблема 4: База данных повреждена

**Решение:**

```bash
# Остановка бота
systemctl stop water_bot

# Резервная копия БД
cp /root/water-reminder-bot/reminders.db /root/reminders.db.backup
cp /root/water-reminder-bot/scheduler_jobs.db /root/scheduler_jobs.db.backup

# Проверка целостности БД
sqlite3 /root/water-reminder-bot/reminders.db "PRAGMA integrity_check;"

# Если повреждена - пересоздание
rm /root/water-reminder-bot/scheduler_jobs.db

# Запуск бота (БД пересоздастся)
systemctl start water_bot
```

---

### Проблема 5: Не могу подключиться по SSH

**Решение:**

1. Проверьте IP-адрес в панели Aeza
2. Проверьте, что сервер запущен (в панели Aeza)
3. Попробуйте другой SSH клиент
4. Сбросьте пароль root в панели Aeza

---

### Проблема 6: Недостаточно места на диске

**Решение:**

```bash
# Проверка свободного места
df -h

# Поиск больших файлов
du -h /root | sort -rh | head -20

# Очистка логов
> /root/water-reminder-bot/bot_log.txt
> /root/water-reminder-bot/systemd_output.log
> /root/water-reminder-bot/systemd_error.log

# Удаление старых бэкапов
rm /root/backups/water-bot-old-*.tar.gz

# Очистка apt кэша
apt clean
apt autoremove -y
```

---

## 📚 Полезные команды

### Системная информация

```bash
# Информация о системе
uname -a

# Время работы сервера
uptime

# Использование диска
df -h

# Использование памяти
free -h

# Список процессов
ps aux

# Сетевые подключения
netstat -tulpn
```

### Работа с файлами

```bash
# Просмотр содержимого файла
cat filename.txt

# Редактирование файла
nano filename.txt

# Поиск текста в файлах
grep "search_text" filename.txt

# Поиск файлов
find /root -name "*.db"

# Копирование файлов
cp source.txt destination.txt

# Перемещение файлов
mv source.txt destination.txt

# Удаление файлов (осторожно!)
rm filename.txt
```

### Работа с архивами

```bash
# Создание архива
tar -czf archive.tar.gz folder/

# Распаковка архива
tar -xzf archive.tar.gz

# Просмотр содержимого архива
tar -tzf archive.tar.gz
```

---

## 🎯 Чеклист успешного развертывания

- [ ] VPS арендован на Aeza
- [ ] Подключение по SSH работает
- [ ] Система обновлена (apt update && upgrade)
- [ ] Python 3.10+ установлен
- [ ] Git установлен
- [ ] Код бота загружен на сервер
- [ ] Виртуальное окружение создано
- [ ] Зависимости установлены (requirements.txt)
- [ ] Файл .env создан и содержит токен
- [ ] Бот запускается вручную (python -m app)
- [ ] Бот отвечает в Telegram на команду /start
- [ ] Systemd service создан и настроен
- [ ] Сервис включен (systemctl enable)
- [ ] Сервис запущен (systemctl start)
- [ ] Статус сервиса = active (running)
- [ ] Логи не содержат ошибок
- [ ] Автозапуск работает после перезагрузки (reboot)
- [ ] Резервное копирование настроено

---

## 🔐 Рекомендации по безопасности

### 1. Настройка брандмауэра

```bash
# Установка UFW
apt install ufw -y

# Разрешение SSH
ufw allow 22/tcp

# Включение firewall
ufw enable

# Проверка статуса
ufw status
```

### 2. Отключение root-логина по SSH (опционально)

```bash
# Сначала создайте обычного пользователя и настройте sudo!
# Потом:
nano /etc/ssh/sshd_config

# Найдите и измените:
# PermitRootLogin yes → PermitRootLogin no

# Перезапуск SSH
systemctl restart sshd
```

### 3. Установка Fail2Ban (защита от брутфорса)

```bash
# Установка
apt install fail2ban -y

# Запуск
systemctl enable fail2ban
systemctl start fail2ban

# Проверка
fail2ban-client status sshd
```

### 4. Регулярное обновление системы

```bash
# Создание скрипта обновления
nano /root/update_system.sh
```

Вставьте:

```bash
#!/bin/bash
apt update
apt upgrade -y
apt autoremove -y
apt clean
```

Сделайте исполняемым и добавьте в cron:

```bash
chmod +x /root/update_system.sh

# Добавление в cron (каждое воскресенье в 4:00)
crontab -e
# 0 4 * * 0 /root/update_system.sh >> /root/update.log 2>&1
```

---

## 📞 Поддержка

### Контакты Aeza

- Сайт: [aeza.net](https://aeza.net)
- Поддержка: через тикет-систему в личном кабинете
- Telegram: [@aeza_hosting](https://t.me/aeza_hosting)

### Полезные ресурсы

- [Документация Ubuntu](https://ubuntu.com/server/docs)
- [Документация python-telegram-bot](https://docs.python-telegram-bot.org/)
- [Документация APScheduler](https://apscheduler.readthedocs.io/)
- [Документация systemd](https://www.freedesktop.org/software/systemd/man/)

---

## ✅ Итог

Поздравляю! Если вы дошли до этого момента и все работает, ваш бот:

- ✅ Работает 24/7 на облачном сервере
- ✅ Автоматически перезапускается при сбоях
- ✅ Запускается после перезагрузки сервера
- ✅ Логирует все события
- ✅ Защищен резервными копиями

**Следующие шаги:**

1. Протестируйте все функции бота
2. Настройте мониторинг (опционально)
3. Пригласите пользователей
4. Регулярно проверяйте логи
5. Делайте резервные копии

---

**Версия документа**: 1.0  
**Последнее обновление**: Октябрь 2025  
**Автор**: Water Reminder Bot Team

**Лицензия**: MIT

---

**Удачи в эксплуатации вашего бота! 🎉**

