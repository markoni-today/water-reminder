# 🔐 Настройка SSH ключей для автоматического деплоя

## Текущий статус

✅ SSH ключ уже существует на вашем компьютере  
📋 Публичный ключ: `ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIKmuMSWMGIG4UmMjmLNPM7HpYkzInkK4PP4S2cPyqCpC mkgrabarev@gmail.com`

---

## Шаг 1: Добавить ключ на сервер

### На вашем компьютере (PowerShell):

```powershell
# Показать публичный ключ
Get-Content "$env:USERPROFILE\.ssh\id_ed25519.pub"
```

**Скопируйте весь вывод** (начинается с `ssh-ed25519...`)

### На сервере (выполните после подключения):

```bash
# Подключитесь к серверу
ssh root@213.108.21.142

# Создать директорию .ssh если её нет
mkdir -p ~/.ssh
chmod 700 ~/.ssh

# Добавить ваш публичный ключ в authorized_keys
echo "ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIKmuMSWMGIG4UmMjmLNPM7HpYkzInkK4PP4S2cPyqCpC mkgrabarev@gmail.com" >> ~/.ssh/authorized_keys

# Установить правильные права
chmod 600 ~/.ssh/authorized_keys

# Проверить что файл создан
cat ~/.ssh/authorized_keys
```

---

## Шаг 2: Проверка подключения без пароля

### С вашего компьютера:

```powershell
# Попробуйте подключиться (пароль не должен запрашиваться)
ssh root@213.108.21.142 "echo 'SSH ключ работает!'"
```

Если видите сообщение "SSH ключ работает!" - всё настроено правильно! ✅

---

## Шаг 3: Автоматический скрипт деплоя

После настройки SSH ключей используйте скрипт `deploy_auto.ps1` для автоматического обновления.

---

## Проблемы?

### Если пароль всё равно запрашивается:

1. Проверьте права на файлы:
```bash
ssh root@213.108.21.142
ls -la ~/.ssh/
# Должно быть:
# drwx------ .ssh
# -rw------- authorized_keys
```

2. Проверьте логи SSH:
```bash
# На сервере
tail -f /var/log/auth.log
# Или
journalctl -u ssh
```

3. Убедитесь что используете правильный ключ:
```powershell
# На локальном компьютере
ssh -v root@213.108.21.142
```

---

## Безопасность

⚠️ **ВАЖНО**: После настройки SSH ключей измените пароль root на сервере:

```bash
passwd root
```

Теперь можно будет подключаться только по ключу, что безопаснее.

