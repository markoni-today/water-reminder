# 💧 Water Reminder Bot

Telegram-бот для напоминаний о питье воды и кастомных напоминаний с поддержкой персистентности задач.

**Версия:** 2.0 | **Статус:** ✅ Production Ready | **Последнее обновление:** Октябрь 2025

---

## 📋 Оглавление

- [Особенности](#-особенности)
- [Требования](#-требования)
- [Быстрый старт (локально)](#-быстрый-старт)
- [Развертывание на сервере](#-развертывание-на-сервере)
  - [Вариант 1: Ручное развертывание](#вариант-1-systemd-рекомендуется-для-linux-vps)
  - [Вариант 2: Автоматический скрипт](#быстрое-развертывание-с-deploysh)
  - [Вариант 3: Docker](#вариант-2-docker)
- [Развертывание на Aeza](#-развертывание-на-aeza)
- [Структура проекта](#-структура-проекта)
- [Использование бота](#-использование)
- [Конфигурация](#️-конфигурация)
- [Устранение неполадок](#-устранение-неполадок)
- [Дополнительная документация](#-дополнительная-документация)

---

## 🌟 Особенности

- ⏰ **Напоминания о воде** с настраиваемым интервалом и рабочим временем
- 📝 **Кастомные напоминания** (разовые, ежедневные, еженедельные)
- 💾 **Персистентность задач** - напоминания сохраняются при перезапуске бота
- 🔄 **Восстановление пропущенных напоминаний** после простоя
- 🌍 **Поддержка часовых поясов**
- 🏗️ **Модульная архитектура** для легкого расширения

## 📋 Требования

- Python 3.13 или выше
- pip (менеджер пакетов Python)
- Telegram Bot Token (от [@BotFather](https://t.me/BotFather))

## 🚀 Быстрый старт

### 1. Клонирование репозитория

```bash
git clone https://github.com/your-username/water-reminder-bot.git
cd water-reminder-bot
```

### 2. Создание виртуального окружения

```bash
# Windows
python -m venv venv
venv\Scripts\activate

# Linux/macOS
python3 -m venv venv
source venv/bin/activate
```

### 3. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 4. Настройка переменных окружения

Скопируйте файл примера и заполните свои данные:

```bash
# Linux/macOS
cp .env.example .env

# Windows
copy .env.example .env
```

Откройте `.env` и добавьте токен вашего бота:

```env
TELEGRAM_BOT_TOKEN=your_bot_token_here
```

### 5. Запуск бота

```bash
# Рекомендуемый способ
python -m app

# Альтернативный способ (для обратной совместимости)
python water_reminder_bot.py
```

## 📦 Структура проекта

```
water-reminder-bot/
├── app/
│   ├── __init__.py
│   ├── __main__.py
│   ├── bot.py                    # Главный файл бота
│   ├── config.py                 # Конфигурация
│   ├── database/                 # Модули работы с БД
│   │   ├── __init__.py
│   │   ├── models.py
│   │   ├── migrations.py
│   │   ├── water_db.py
│   │   └── custom_db.py
│   ├── scheduler/                # Планировщик задач
│   │   ├── __init__.py
│   │   ├── job_manager.py
│   │   └── async_wrapper.py
│   ├── handlers/                 # Обработчики команд
│   │   ├── __init__.py
│   │   ├── start.py
│   │   ├── water_handlers.py
│   │   └── custom_handlers.py
│   └── utils/                    # Утилиты
│       ├── __init__.py
│       └── logger.py
├── .env.example                  # Пример конфигурации
├── .gitignore
├── requirements.txt              # Зависимости
├── water_reminder_bot.py         # Точка входа (deprecated)
└── README.md                     # Эта документация
```

## 🐳 Развертывание на сервере

### Вариант 1: systemd (рекомендуется для Linux VPS)

1. Создайте сервисный файл:

```bash
sudo nano /etc/systemd/system/water_bot.service
```

2. Добавьте конфигурацию:

```ini
[Unit]
Description=Water Reminder Telegram Bot
After=network.target

[Service]
Type=simple
User=your_username
WorkingDirectory=/path/to/water-reminder-bot
Environment="PATH=/path/to/water-reminder-bot/venv/bin"
ExecStart=/path/to/water-reminder-bot/venv/bin/python -m app
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

3. Активируйте и запустите сервис:

```bash
sudo systemctl daemon-reload
sudo systemctl enable water_bot
sudo systemctl start water_bot
sudo systemctl status water_bot
```

### Быстрое развертывание с deploy.sh

Для автоматического развертывания используйте скрипт `deploy.sh`:

```bash
# Загрузите проект на сервер
git clone https://github.com/your-username/water-reminder-bot.git
cd water-reminder-bot

# Запустите автоматический скрипт
sudo bash deploy.sh
```

Скрипт автоматически:
- ✅ Обновит систему
- ✅ Установит все зависимости
- ✅ Создаст виртуальное окружение
- ✅ Запросит токен бота
- ✅ Создаст systemd сервис
- ✅ Запустит бота

**Время развертывания:** ~10-15 минут

---

### Вариант 2: Docker

```dockerfile
FROM python:3.13-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

CMD ["python", "-m", "app"]
```

Запуск:

```bash
docker build -t water-reminder-bot .
docker run -d --name water_bot --env-file .env water-reminder-bot

# Или с docker-compose:
docker-compose up -d
```

### Вариант 3: Screen/tmux (простой способ)

```bash
# С помощью screen
screen -S water_bot
python -m app
# Нажмите Ctrl+A затем D для отключения

# С помощью tmux
tmux new -s water_bot
python -m app
# Нажмите Ctrl+B затем D для отключения
```

## 🌐 Развертывание на Aeza

### Пошаговая инструкция:

1. **Аренда VPS на Aeza**
   - Зарегистрируйтесь на [aeza.net](https://aeza.net)
   - Закажите VPS (минимум 1 CPU, 512MB RAM, 10GB SSD)
   - Выберите Ubuntu 22.04 LTS

2. **Подключение к серверу**

```bash
ssh root@your_server_ip
```

3. **Установка необходимого ПО**

```bash
# Обновление системы
apt update && apt upgrade -y

# Установка Python и Git
apt install python3 python3-pip python3-venv git -y
```

4. **Загрузка проекта**

```bash
# Если используете Git
git clone https://github.com/your-username/water-reminder-bot.git
cd water-reminder-bot

# Или загрузите файлы через SCP/SFTP
```

5. **Настройка окружения**

```bash
# Создание виртуального окружения
python3 -m venv venv
source venv/bin/activate

# Установка зависимостей
pip install -r requirements.txt

# Создание .env файла
nano .env
# Вставьте ваш TELEGRAM_BOT_TOKEN
```

6. **Настройка автозапуска (systemd)**

```bash
nano /etc/systemd/system/water_bot.service
```

Вставьте:

```ini
[Unit]
Description=Water Reminder Telegram Bot
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/root/water-reminder-bot
ExecStart=/root/water-reminder-bot/venv/bin/python -m app
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

Запустите:

```bash
systemctl daemon-reload
systemctl enable water_bot
systemctl start water_bot
systemctl status water_bot
```

7. **Просмотр логов**

```bash
# В реальном времени
journalctl -u water_bot -f

# Или из файла
tail -f bot_log.txt
```

## 📝 Использование

### Команды бота

- `/start` - Главное меню
- `/reset` - Сброс всех настроек
- `/cancel` - Отмена текущего действия

### Функции

1. **Напоминания о воде**
   - Настройка интервала (30-240 минут)
   - Установка рабочего времени
   - Персональное сообщение

2. **Кастомные напоминания**
   - Разовые напоминания
   - Ежедневные повторения
   - Еженедельные повторения
   - Просмотр активных напоминаний
   - Удаление напоминаний

## ⚙️ Конфигурация

Все настройки в файле `.env`:

```env
# Обязательные
TELEGRAM_BOT_TOKEN=your_token

# Опциональные
DEFAULT_TIMEZONE=Etc/GMT-3
DB_NAME=reminders.db
LOG_LEVEL=INFO
MAX_CUSTOM_REMINDERS=10
DEBUG_MODE=false
```

## 🔧 Устранение неполадок

### Бот не запускается

```bash
# Проверьте токен
cat .env | grep TELEGRAM_BOT_TOKEN

# Проверьте зависимости
pip list | grep -E "python-telegram-bot|APScheduler"

# Проверьте логи
tail -50 bot_log.txt
```

### Напоминания не работают

```bash
# Проверьте статус планировщика в логах
grep "scheduler" bot_log.txt

# Проверьте базу данных
ls -lh *.db
```

### Обновление бота на сервере

```bash
cd /path/to/water-reminder-bot
git pull
systemctl restart water_bot
```

## 🧪 Тестирование

Запустите тесты:

```bash
python test_bot_qa.py
```

## 📊 Мониторинг

### Просмотр статуса

```bash
systemctl status water_bot
```

### Просмотр использования ресурсов

```bash
ps aux | grep python
```

### Анализ логов

```bash
# Последние 100 строк
tail -100 bot_log.txt

# Поиск ошибок
grep ERROR bot_log.txt

# Статистика
wc -l bot_log.txt
```

## 🔒 Безопасность

- ✅ `.env` файл добавлен в `.gitignore`
- ✅ Токен бота не хранится в коде
- ✅ Базы данных локальные (SQLite)
- ✅ Нет открытых портов (только polling)

## 📚 Дополнительная документация

### Руководства по развертыванию:

- **[AEZA_DEPLOYMENT.md](AEZA_DEPLOYMENT.md)** - 📘 Подробное руководство по развертыванию на Aeza (1000+ строк)
  - Пошаговые инструкции от регистрации до запуска
  - Настройка автозапуска через systemd
  - Мониторинг и обслуживание
  - Резервное копирование
  - Устранение всех возможных проблем
  
- **[БЫСТРЫЙ_ДЕПЛОЙ_AEZA.md](БЫСТРЫЙ_ДЕПЛОЙ_AEZA.md)** - ⚡ Краткая инструкция для опытных (5 шагов за 15 минут)

- **[CHECKLIST_DEPLOYMENT.md](CHECKLIST_DEPLOYMENT.md)** - ✅ Чеклист из 100+ пунктов для проверки готовности

- **[DEPLOYMENT_SUMMARY.md](DEPLOYMENT_SUMMARY.md)** - 📊 Итоговый анализ готовности и рекомендации

### Скрипты и конфигурация:

- `deploy.sh` - Автоматический скрипт развертывания
- `Dockerfile` - Docker образ для контейнеризации
- `docker-compose.yml` - Docker Compose конфигурация
- `env.example` - Пример файла конфигурации

## 🤝 Вклад в проект

1. Fork репозитория
2. Создайте ветку для новой функции (`git checkout -b feature/AmazingFeature`)
3. Commit изменений (`git commit -m 'Add some AmazingFeature'`)
4. Push в ветку (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 📄 Лицензия

Этот проект распространяется под лицензией MIT.

## 📧 Контакты

- Telegram: [@your_telegram](https://t.me/your_telegram)
- Email: your.email@example.com

## 🎉 Благодарности

- [python-telegram-bot](https://github.com/python-telegram-bot/python-telegram-bot) - Telegram Bot API wrapper
- [APScheduler](https://github.com/agronholm/apscheduler) - Advanced Python Scheduler

---

**Версия**: 2.0  
**Статус**: ✅ Production Ready  
**Последнее обновление**: Октябрь 2025

