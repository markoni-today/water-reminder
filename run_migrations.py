"""
Скрипт для запуска миграций базы данных
"""
import sys
import os

# Добавляем путь к модулю app
sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))

from app.database import init_db, run_all_migrations
from app.utils import setup_logger

# Настраиваем логирование
logger = setup_logger(__name__, 'INFO', None)

def main():
    """Инициализирует БД и запускает миграции."""
    print("=" * 80)
    print("  ЗАПУСК МИГРАЦИЙ БАЗЫ ДАННЫХ")
    print("=" * 80)
    print()
    
    try:
        # Инициализируем БД (создаём таблицы если их нет)
        print("📦 Инициализация базы данных...")
        init_db()
        print("✅ База данных инициализирована")
        print()
        
        # Запускаем миграции
        print("🔄 Запуск миграций...")
        run_all_migrations()
        print()
        
        print("=" * 80)
        print("  ✅ ВСЕ МИГРАЦИИ ВЫПОЛНЕНЫ УСПЕШНО")
        print("=" * 80)
        return 0
        
    except Exception as e:
        print()
        print("=" * 80)
        print(f"  ❌ ОШИБКА ПРИ ВЫПОЛНЕНИИ МИГРАЦИЙ: {e}")
        print("=" * 80)
        return 1

if __name__ == "__main__":
    sys.exit(main())

